particles: ["로", "가", "는", "은", "을", "를", "의", "에", "이"]
content_word_homophones: ["키", "셋", "원", "풀", "풋", "패스", "베이스"]
known_terms:   # 수동 curated 테이블 — 항상 최우선
  Row: {korean: "로우", label: particle_collision}
  Key: {korean: "키", label: content_word_collision}
  Set: {korean: "셋", label: content_word_collision}
  One: {korean: "원", label: content_word_collision}
  Primary Key: {korean: "프라이머리 키", label: content_word_collision}
  Foreign Key: {korean: "포린 키", label: content_word_collision}
  # 아래는 CS기초.pdf / Git.pdf 기반으로 추가한 항목 — 실측(실제 오디오) 검증 전 상태
  Local: {korean: "로컬", label: particle_collision}
  API: {korean: "에이피아이", label: particle_collision}
  Pull: {korean: "풀", label: content_word_collision}
  Pull Request: {korean: "풀 리퀘스트", label: content_word_collision}
  API Key: {korean: "에이피아이 키", label: content_word_collision}
  # 아래는 Web.pdf / 크롤링.pdf 기반으로 추가한 항목 — 실측(실제 오디오) 검증 전 상태
  URI: {korean: "유알아이", label: particle_collision}   # 끝음절 "이"가 주격조사 "이"와 충돌
  SPA: {korean: "에스피에이", label: particle_collision}  # 끝음절 "이"가 주격조사 "이"와 충돌
  PUT: {korean: "풋", label: content_word_collision}      # "풋(사과/내기)"의 접두 형태소와 충돌
  Path: {korean: "패스", label: content_word_collision}   # 일상어 "패스"(통과/생략)와 충돌
  Base: {korean: "베이스", label: content_word_collision}  # 일상어 "베이스"(악기/화장품 베이스)와 충돌
  # 아래는 기초통계.pdf / EDA_FE.pdf 기반으로 추가한 항목 — 실측(실제 오디오) 검증 전 상태
  # pdf_extract.py + term_candidates.py를 그대로 돌려서 나온 후보 중,
  # 발음 시작/끝 음절이 particles 목록과 겹치거나(Row/Local/API/URI/SPA와 동일한 패턴)
  # 전체 발음이 content_word_homophones와 겹치는 것만 수동으로 골라냄.
  Logistic: {korean: "로지스틱", korean_variants: ["로지스틱"], label: particle_collision}  # 시작음절 "로"가 조사 "로"와 충돌 (Local과 동일 패턴)
  Robust: {korean: "로버스트", korean_variants: ["로버스트"], label: particle_collision}    # 시작음절 "로"가 조사 "로"와 충돌
  Loss: {korean: "로스", korean_variants: ["로스"], label: particle_collision}              # 시작음절 "로"가 조사 "로"와 충돌 + "로스구이"의 로스와 동음이의 가능성도 있음(2중 위험)
  Garbage: {korean: "가비지", korean_variants: ["가비지"], label: particle_collision}        # 시작음절 "가"가 주격조사 "가"와 충돌
  Shapiro: {korean: "샤피로", korean_variants: ["샤피로", "셔피로"], label: particle_collision}  # 끝음절 "로"가 조사 "로"와 충돌 (Row/Local과 반대로 끝에 옴 — 새 서브패턴, 검증 우선순위 높음)
  PCA: {korean: "피씨에이", korean_variants: ["피씨에이"], label: particle_collision}         # 끝음절 "에이"가 주격조사 "이"와 충돌 (SPA와 동일 패턴)
  EDA: {korean: "이디에이", korean_variants: ["이디에이"], label: particle_collision}         # 끝음절 "에이"가 주격조사 "이"와 충돌
  LDA: {korean: "엘디에이", korean_variants: ["엘디에이"], label: particle_collision}         # 끝음절 "에이"가 주격조사 "이"와 충돌
  DA: {korean: "디에이", korean_variants: ["디에이"], label: particle_collision}             # 끝음절 "에이"가 주격조사 "이"와 충돌 ("DA Interview" 챕터에서 반복 등장)
  RFE: {korean: "알에프이", korean_variants: ["알에프이"], label: particle_collision}         # 끝글자 E가 "이"로 읽혀 주격조사 "이"와 직접 충돌 (SPA류보다 더 명확한 케이스)
  # 아래는 시각화.pdf / 3-2_DL.pdf 기반으로 추가한 항목 — 실측(실제 오디오) 검증 전 상태
  # 동일하게 pdf_extract.py + term_candidates.py 후보 중 particles/content_word_homophones와
  # 겹치는 것만 수동으로 골라냄.
  Raw: {korean: "로우", korean_variants: ["로우"], label: particle_collision}  # 시작음절 "로"가 조사 "로"와 충돌 (Row와 완전히 동일한 패턴). 시각화.pdf "Raw data를 이해하는 과정"에서 실제 등장
  Log: {korean: "로그", korean_variants: ["로그"], label: particle_collision}  # 시작음절 "로"가 조사 "로"와 충돌 (Local/Loss와 동일 패턴이나 별개 단어이므로 별도 등록 필요). DL.pdf "Log Cosh Loss"
  Pie: {korean: "파이", korean_variants: ["파이"], label: particle_collision}  # 끝음절 "이"가 주격조사 "이"와 충돌 (SPA/PCA류 패턴)
  AI: {korean: "에이아이", korean_variants: ["에이아이"], label: particle_collision}  # 끝음절 "이"가 주격조사 "이"와 충돌
  QA: {korean: "큐에이", korean_variants: ["큐에이"], label: particle_collision}  # 끝음절 "에이"가 주격조사 "이"와 충돌 (PCA/EDA류)
  VAE: {korean: "브이에이이", korean_variants: ["브이에이이", "브이에이"], label: particle_collision}  # 끝글자 E가 "이"로 읽혀 주격조사 "이"와 직접 충돌 (RFE류)
  L1: {korean: "엘원", korean_variants: ["엘원"], label: content_word_collision}  # "원"(One)과 동일 발음 충돌 — 숫자를 영어식으로 읽는 관행(엘원/엘투) 때문에 발생
  # 아래는 ML.pdf(2026 Summer YBIGTA 머신러닝) 기반으로 추가한 항목 — 실측(실제 오디오) 검증 전 상태
  # 동일하게 pdf_extract.py + term_candidates.py 후보 중, particles/content_word_homophones와
  # 겹치는 것만 수동으로 골라냄. (What We Have/What We Must Decide 등 슬라이드 부제에서 나온
  # Have/Must/Decide/WWH/WWMD 같은 후보는 실제 전문용어가 아니라 문구 파편이라 제외)
  F1: {korean: "에프원", korean_variants: ["에프원"], label: content_word_collision}  # 끝음절 "원"이 One과 동일 발음 충돌 (L1과 동일 패턴). "F1 Score" 표현에서 반복 등장
  FE: {korean: "에프이", korean_variants: ["에프이"], label: particle_collision}  # 끝글자 E가 "이"로 읽혀 주격조사 "이"와 직접 충돌 (RFE/DA류와 동일 패턴). "EDA·FE" 표현에서 반복 등장
  CV: {korean: "씨브이", korean_variants: ["씨브이"], label: particle_collision}  # 끝음절이 "이"로 끝나 조사(가/이)와 충돌 가능 (VAE류와 유사한 서브패턴). "5-Fold CV라면" 표현에서 등장
  # 아래는 NLP.pdf(2026-2 YBIGTA NLP) / CV.pdf(2026 Summer YBIGTA CV) 기반으로 추가한 항목 — 실측(실제 오디오) 검증 전 상태
  # 동일하게 pdf_extract.py + term_candidates.py 후보(NLP.pdf 130개, CV.pdf 135개) 중, particles/content_word_homophones와
  # 겹치는 것만 수동으로 골라냄.
  Roberta: {korean: "로버타", korean_variants: ["로버타"], label: particle_collision}  # 시작음절 "로"가 조사 "로"와 충돌 (Local/Loss와 동일 패턴). "Sub task Fine Tuning : Roberta, finbert, kobert.." 슬라이드에서 등장
  Rotation: {korean: "로테이션", korean_variants: ["로테이션"], label: particle_collision}  # 시작음절 "로"가 조사 "로"와 충돌 (Roberta와 동일 패턴)
  Gaussian: {korean: "가우시안", korean_variants: ["가우시안"], label: particle_collision}  # 시작음절 "가"가 주격조사 "가"와 충돌 (Garbage와 동일 패턴). "Gaussian Blur" 표현에서 등장
  PMI: {korean: "피엠아이", korean_variants: ["피엠아이"], label: particle_collision}  # 끝음절이 "이"로 끝나 주격조사 "이"와 충돌 (PCA/EDA류와 동일 패턴)
  PPMI: {korean: "피피엠아이", korean_variants: ["피피엠아이"], label: particle_collision}  # 끝음절이 "이"로 끝나 주격조사 "이"와 충돌 (PMI와 동일 패턴)
  Output: {korean: "아웃풋", korean_variants: ["아웃풋"], label: content_word_collision}  # 끝음절 "풋"이 PUT과 동일 음절 충돌이나, 어두가 아닌 어말이라 위험도는 상대적으로 낮게 평가 — 검증 우선순위 낮음
  Fast: {korean: "패스트", korean_variants: ["패스트"], label: content_word_collision}  # 시작 2음절 "패스"가 일상어 "패스"(통과/생략)와 충돌 (Path와 동일 패턴). "Fast/Faster R-CNN" 표현에서 등장
  Faster: {korean: "패스터", korean_variants: ["패스터"], label: content_word_collision}  # 시작 2음절 "패스"가 일상어 "패스"와 충돌 (Fast와 동일 패턴)
  Pooling: {korean: "풀링", korean_variants: ["풀링"], label: content_word_collision}  # 시작음절 "풀"이 Pull과 동일 발음 충돌. CNN 구조에서 반복 등장하는 핵심 용어
  Image: {korean: "이미지", korean_variants: ["이미지"], label: particle_collision}  # 시작음절 "이"가 주격조사 "이"와 충돌 가능한 패턴이나, 매우 흔한 일상어라 오탐(false positive) 가능성이 높음 — 검증 우선순위 가장 낮음, 실측에서 우선 제외 검토
  # 아래는 4-2_LLM.pdf(2026 Summer YBIGTA LLM) 기반으로 추가한 항목 — 실측(실제 오디오) 검증 전 상태
  # 동일하게 pdf_extract.py + term_candidates.py 후보(705개, 이름/수식 표기/이미 등록된 약어 다수 포함) 중,
  # particles/content_word_homophones와 겹치는 실제 전문용어만 수동으로 골라냄.
  # RLHF/DPO/GPT/BERT/PPO/KTO/RM/CoT/SFT/RAG 등은 후보에 있었지만 끝/시작 음절이
  # 조사(로/가/이)나 content_word_homophones와 겹치지 않아 제외.
  VLA: {korean: "브이엘에이", korean_variants: ["브이엘에이"], label: particle_collision}  # 끝음절 "에이"가 주격조사 "이"와 충돌 (SPA/PCA/QA류와 동일 패턴). "VLA (Vision-Language-Action)" 슬라이드에서 등장
  GAE: {korean: "지에이이", korean_variants: ["지에이이", "지에이"], label: particle_collision}  # 끝글자 E가 "이"로 읽혀 주격조사 "이"와 직접 충돌 (RFE/DA/VAE류와 동일 패턴). PPO/GRPO 비교 다이어그램에서 등장
  TriviaQA: {korean: "트리비아큐에이", korean_variants: ["트리비아큐에이"], label: particle_collision}  # 끝음절 "에이"가 주격조사 "이"와 충돌 (QA를 포함하는 복합어라 QA와 동일 패턴). In-Context Learning 성능 그래프(벤치마크명)에서 등장
  GPQA: {korean: "지피큐에이", korean_variants: ["지피큐에이"], label: particle_collision}  # 끝음절 "에이"가 주격조사 "이"와 충돌 (QA류와 동일 패턴). "GPQA Diamond" 벤치마크로 o1 성능 그래프에서 등장
  # 아래는 0731_Docker.pdf / 0804_AWS_교육세션_발제.pdf 기반으로 추가한 항목 — 실측(실제 오디오) 검증 전 상태
  # pdf_extract.py + extract_and_dedup으로 페이지 중복 제거 후, term_candidates.py의
  # extract_candidate_terms + filter_function_words를 그대로 돌린 후보(Docker 62개, AWS 74개) 중,
  # 발음 시작/끝 음절이 particles와 겹치거나 content_word_homophones와 겹치는 것만 수동으로 골라냄.
  # Docker.pdf에서는 새로 추가할 만한 강한 후보가 없었음 (GUI가 "구이"로 들릴 가능성이 있으나
  # 실제로는 "지유아이"로 읽히는 경우가 더 흔해 보류 — 검증 시 우선순위 가장 낮게 확인 필요).
  EC2: {korean: "이씨투", korean_variants: ["이씨투", "이씨이"], label: particle_collision}  # 시작음절 "이"가 주격조사 "이"와 충돌 (Image와 동일 패턴). AWS.pdf 전체에서 가장 빈번히 등장하는 핵심 서비스명 — 검증 최우선
  EBS: {korean: "이비에스", korean_variants: ["이비에스"], label: particle_collision}  # 시작음절 "이"가 주격조사 "이"와 충돌 (EC2와 동일 패턴)
  ELB: {korean: "이엘비", korean_variants: ["이엘비"], label: particle_collision}  # 시작음절 "이"가 주격조사 "이"와 충돌 (EC2/EBS와 동일 패턴). Auto Scaling 설명에서 등장
  AMI: {korean: "에이엠아이", korean_variants: ["에이엠아이"], label: particle_collision}  # 끝음절 "아이"의 마지막 소리 "이"가 주격조사 "이"와 충돌 (API류와 동일 패턴). "AMI(Amazon Machine Image)" 슬라이드에서 반복 등장
  CI: {korean: "씨아이", korean_variants: ["씨아이"], label: particle_collision}  # 끝음절 "아이"의 마지막 소리 "이"가 주격조사 "이"와 충돌 (AMI류와 동일 패턴). "CI/CD 파이프라인" 표현에서 등장
  Load: {korean: "로드", korean_variants: ["로드"], label: particle_collision}  # 시작음절 "로"가 조사 "로"와 충돌 (Local/Log/Loss와 동일 패턴). "Load Balancer" 표현에서 반복 등장
  Database: {korean: "데이터베이스", korean_variants: ["데이터베이스"], label: content_word_collision}  # 끝음절 "베이스"가 이미 등록된 Base(일상어 "베이스")와 동일하게 충돌 — Log Cosh Loss처럼 복합어 꼬리 충돌 케이스
  CIDR: {korean: "씨아이디알", korean_variants: ["씨아이디알", "사이더"], label: content_word_collision}  # 네트워킹 용어 관행상 "사이더(cider)"로 읽히는 경우가 있어 음료 "사이다"와 동음이의 위험 — 스펠아웃("씨아이디알")으로 읽으면 충돌 없음. 실제 발화 방식 확인이 검증 시 최우선 과제
  Pay: {korean: "페이", korean_variants: ["페이"], label: particle_collision}  # 끝음절 "이"가 주격조사 "이"와 충돌 (Pie와 동일 패턴). "Pay-as-You-Go" 표현에서 1회 등장 — 검증 우선순위 낮음
  # 아래는 0807_RAG.pdf / 0807_AI_Agent.pdf 기반으로 추가한 항목 — 실측(실제 오디오) 검증 전 상태
  # pdf_extract.py의 extract_and_dedup으로 페이지 중복 제거 후, term_candidates.py의
  # extract_candidate_terms + filter_function_words를 그대로 돌린 후보(RAG 120개, AI_Agent 284개) 중,
  # 발음 끝음절이 particles("로/가/이")와 겹치거나 content_word_homophones와 겹치는 것만 수동으로 골라냄.
  # 이 두 PDF에서는 "시작음절 로/가" 패턴이나 content_word_homophones 매치는 발견되지 않았고,
  # 전부 "끝음절 이(아이/에이 포함)" 패턴만 해당됨. RAG/BERT/GPT/CoT/LLM/MCP/DB 등은 후보에 있었지만
  # 끝/시작 음절이 조사(로/가/이)와 겹치지 않아 제외.
  A2A: {korean: "에이투에이", korean_variants: ["에이투에이"], label: particle_collision}  # 끝음절 "이"가 주격조사 "이"와 충돌 (EC2/AMI류와 동일 패턴). "Agent2Agent Protocol" 비교표에서 반복 등장
  ADK: {korean: "에이디케이", korean_variants: ["에이디케이"], label: particle_collision}  # 끝음절 "이"가 주격조사 "이"와 충돌 (AMI/CI류와 동일 패턴). Google-ADK 섹션 전체(슬라이드 4장)에서 최빈출 — 검증 최우선
  CLI: {korean: "씨엘아이", korean_variants: ["씨엘아이"], label: particle_collision}  # 끝음절 "아이"의 마지막 소리 "이"가 주격조사 "이"와 충돌 (CI류와 동일 패턴). "Claude code, Gemini CLI" 표현에서 등장
  Gemini: {korean: "제미나이", korean_variants: ["제미나이", "제미니"], label: particle_collision}  # 끝음절 "이"가 주격조사 "이"와 충돌 (Roberta류와 달리 끝에서 발생, Shapiro와 유사 서브패턴). "Gemini CLI" 표현에서 등장
  Phi: {korean: "파이", korean_variants: ["파이"], label: particle_collision}  # 끝음절 "이"가 주격조사 "이"와 충돌. 이미 등록된 Pie(파이)와 발음이 완전히 동일한 별개 단어 — Raw/Row 같은 동음 이표기 케이스. "Phi-3-mini" 표현에서 등장
  UI: {korean: "유아이", korean_variants: ["유아이"], label: particle_collision}  # 끝음절 "아이"의 마지막 소리 "이"가 주격조사 "이"와 충돌 (API/CI류와 동일 패턴). "웹/UI 디자인" 표현에서 등장
  E2E: {korean: "이투이", korean_variants: ["이투이"], label: particle_collision}  # 끝음절 "이"가 주격조사 "이"와 충돌. 다만 실제 발화 시 "이투이"로 스펠아웃될지 불확실해 애매성이 큼 — 검증 우선순위 낮음. "회귀적 E2E test" 표현에서 1회 등장
  OpenAI: {korean: "오픈에이아이", korean_variants: ["오픈에이아이"], label: particle_collision}  # 끝음절 "아이"의 마지막 소리 "이"가 주격조사 "이"와 충돌. 이미 등록된 AI와 동일 패턴이라 사실상 중복 위험 — 검증 우선순위 낮음. "OpenAI의 text-embedding-3" 표현에서 1회 등장
