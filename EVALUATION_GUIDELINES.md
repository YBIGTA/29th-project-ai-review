# LLM RAG 평가 지침

이 문서는 STT 결과를 LLM으로 보정한 뒤, RAG 기반 평가 모델이 사용자의 구술 복습 답변을 일관되게 평가하기 위한 기준이다.

평가 모델은 원문에 존재하는 정보만 바탕으로 판단해야 하며, 사용자가 말하지 않은 개념을 임의로 추가하거나 보완해서는 안 된다.

## 1. 입력 데이터

평가 모델은 다음 정보를 입력으로 받는 것을 전제로 한다.

- `raw_transcript`: STT 모델이 생성한 원본 전사 텍스트
- `corrected_transcript`: LLM 보정을 거친 전사 텍스트
- `postprocessed_transcript`: 평가를 위해 최소한으로 정리된 텍스트
- `term_db`: PDF 학습 자료에서 추출한 핵심 단어 DB
- `reference_context`: PDF/RAG 검색을 통해 가져온 관련 학습 자료 내용

## 2. 후처리 원칙

후처리는 평가 모델이 텍스트를 안정적으로 읽을 수 있도록 문장 구조를 정리하는 단계이다.

후처리 과정에서는 의미를 바꾸거나 내용을 보강하지 않는다.

### 허용되는 작업

- 불필요한 미사여구 제거
- 지나친 반복 제거
- 같은 문장 단순 반복 제거
- 문장 분리
- 띄어쓰기 정리
- 문장부호 정리
- 문장 단위 가독성 개선

### 금지되는 작업

- 말하지 않은 개념 추가
- 잘못 말한 내용을 임의로 정정
- 새로운 예시 추가
- 답변 임의 요약
- 중복을 제외한 내용 임의 삭제
- 문맥상 자연스럽게 보이도록 내용 재작성
- 확실하지 않은 단어 임의 수정
- 문장 순서와 논리 구조 임의 변경
- 원문에 존재하지 않는 정보 포함


확실하지 않은 단어는 임의로 수정하지 말고 원문 표현을 유지한다.
전문 용어로 보정할 수 있더라도, 원문 발화와의 대응이 불분명하면 보정하지 않는다.

## 3. 평가 점수 체계

최종 점수는 100점 만점으로 산정.

| 평가 영역 | 배점 |
| --- | ---: |
| 충실성 | 40 |
| 연결성 | 30 |
| 포괄성 | 30 |
| 총점 | 100 |

평가 기준은 모델 변경 후에도 비교할 수 있도록 항상 동일하게 유지한다.

## 4. 평가 영역별 루브릭

### 4.1 충실성

충실성은 사용자가 핵심 단어를 잘 파악하고 설명했는지 평가한다.

핵심 단어 판단은 `term_db`를 기준으로 한다.

| 단계 | 기준 |
| ---: | --- |
| 0단계 | 핵심 단어를 전혀 언급하지 않음 |
| 1단계 | 핵심 단어를 언급함 |
| 2단계 | 핵심 단어의 기본 개념을 설명함 |
| 3단계 | 핵심 단어와 관련된 개념도 설명함 |
| 4단계 | 핵심 단어를 적절한 맥락과 관계 속에서 정확하게 설명함 |

충실성 점수는 핵심 단어별 단계 평가를 바탕으로 40점 만점으로 환산한다.

### 4.2 연결성

연결성은 사용자가 개념 간 관계를 올바르게 이해하고 설명했는지 평가한다.

관계에는 인과 관계, 목적 관계, 차이, 포함 관계, 순서, 구성 요소 간 관계 등이 포함된다.

| 단계 | 기준 |
| ---: | --- |
| 0단계 | 단어를 언급하지만 관계를 설명하지 못함 |
| 1단계 | 개념 사이의 관계를 설명하지만 불완전하거나 모호함 |
| 2단계 | 인과, 목적, 차이, 포함 관계 등을 정확하게 설명함 |
| 3단계 | 여러 개념의 관계를 일관된 논리 구조로 정확하게 설명함 |

연결성 점수는 개념 관계 설명의 정확성과 논리적 일관성을 바탕으로 30점 만점으로 환산한다.

### 4.3 포괄성

포괄성은 사용자가 전체 학습 범위 중 어느 정도를 다루었는지 평가한다.

RAG가 제공한 `reference_context`와 `term_db`를 기준으로 주요 주제의 누락 여부를 판단한다.

| 단계 | 기준 |
| ---: | --- |
| 0단계 | 전체 주제 중 극히 일부만 언급 |
| 1단계 | 주요 주제 중 일부만 제한적으로 다룸 |
| 2단계 | 전반적인 주제를 잘 설명함 |
| 3단계 | 모든 주요 주제를 빠짐없이 다루고, 핵심 영역과 보조 내용을 잘 구분하여 설명함 |

포괄성 점수는 주요 주제 커버리지와 설명 균형을 바탕으로 30점 만점으로 환산한다.

## 5. 정량적 평가 항목

평가 결과에는 다음 정량 지표를 포함한다.

### Corrected CER

LLM 보정 후 전사 텍스트의 문자 오류율이다.

값이 낮을수록 전사 결과가 기준 텍스트에 가깝다.

### Corrected Recall

기준 핵심 단어 중 보정된 전사 결과에 포함된 핵심 단어의 비율이다.

값이 높을수록 사용자가 주요 개념을 많이 언급한 것으로 볼 수 있다.

### Corrected Precision

보정된 전사 결과에서 핵심 단어로 탐지된 항목 중 실제 기준 핵심 단어와 일치하는 비율이다.

값이 높을수록 불필요하거나 잘못 탐지된 핵심 단어가 적다.

### Corrected F1 Score

Corrected Recall과 Corrected Precision의 조화 평균이다.

핵심 단어를 많이 언급하는 것만으로 점수가 높아지는 문제를 줄이기 위해 사용한다.

### 루브릭 점수

다음 점수를 함께 제공한다.

- 충실성 점수
- 연결성 점수
- 포괄성 점수
- 최종 점수

각 점수는 반드시 해당 판단 기준과 함께 제시한다.

## 6. 정성적 평가 항목

평가 결과에는 다음 정성 피드백을 포함한다.

### 누락된 개념

사용자가 설명하지 않았거나 충분히 다루지 않은 핵심 개념을 제시한다.

### 잘못 연결된 개념

개념 간 관계를 잘못 설명했거나, 인과 관계와 포함 관계 등을 혼동한 부분을 지적한다.

### 복습 방향

사용자가 어떤 개념을 어떤 순서로 다시 학습하면 좋을지 제시한다.

복습 방향은 구체적으로 작성하되, 사용자가 말하지 않은 내용을 근거로 점수를 보정해서는 안 된다.

## 7. 평가 출력 형식

평가 모델은 다음 JSON 형식으로 결과를 반환한다.

```json
{
  "quantitative": {
    "corrected_cer": 0.0,
    "corrected_recall": 0.0,
    "corrected_precision": 0.0,
    "corrected_f1": 0.0,
    "scores": {
      "faithfulness": {
        "score": 0,
        "max_score": 40,
        "reason": ""
      },
      "connectivity": {
        "score": 0,
        "max_score": 30,
        "reason": ""
      },
      "comprehensiveness": {
        "score": 0,
        "max_score": 30,
        "reason": ""
      },
      "total": {
        "score": 0,
        "max_score": 100
      }
    }
  },
  "qualitative": {
    "missing_concepts": [],
    "misconnected_concepts": [],
    "review_suggestions": []
  },
  "rubric_levels": {
    "faithfulness": [],
    "connectivity": "",
    "comprehensiveness": ""
  }
}
```

## 8. 평가 시 주의사항

- 후처리된 텍스트에 없는 정보를 근거로 점수를 부여하지 않는다.
- RAG 검색 결과에 있는 내용이더라도 사용자가 말하지 않았다면 사용자의 답변으로 간주하지 않는다.
- LLM 보정이 추가한 것으로 의심되는 개념은 평가 근거로 사용하지 않는다.
- 핵심 단어를 단순 나열한 경우에는 충실성 1단계 이상으로 과대평가하지 않는다.
- 개념 설명이 맞더라도 관계 설명이 부족하면 연결성 점수를 제한한다.
- 일부 핵심 개념을 깊게 설명했더라도 전체 범위를 다루지 못했다면 포괄성 점수를 제한한다.
- 피드백은 사용자의 발화 내용에 근거하여 작성한다.
