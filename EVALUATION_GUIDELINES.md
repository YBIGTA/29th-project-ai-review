# LLM RAG 평가 지침

이 문서는 STT 결과를 LLM으로 보정한 뒤, RAG 기반 평가 모델이 사용자의 구술 복습 답변을 일관되게 평가하기 위한 기준이다.

평가 모델은 원문에 존재하는 정보만 바탕으로 판단해야 하며, 사용자가 말하지 않은 개념을 임의로 추가하거나 보완해서는 안 된다.

## 1. 입력 데이터

평가 모델은 다음 정보를 입력으로 받는 것을 전제로 한다.

- `raw_transcript`: STT 모델이 생성한 원본 전사 텍스트
- `corrected_transcript`: LLM 보정을 거친 전사 텍스트
- `postprocessed_transcript`: 평가를 위해 최소한으로 정리된 텍스트
- `term_db`: PDF 학습 자료에서 추출한 핵심 단어 DB
- `reference_context`: PDF/RAG 검색을 통해 가져온 관련 학습 자료 내용

## 2. 후처리 원칙

후처리는 평가 모델이 텍스트를 안정적으로 읽을 수 있도록 문장 구조를 정리하는 단계이다.

후처리 과정에서는 의미를 바꾸거나 내용을 보강하지 않는다.

### 허용되는 작업

- 불필요한 미사여구 제거
- 지나친 반복 제거
- 같은 문장 단순 반복 제거
- 문장 분리
- 띄어쓰기 정리
- 문장부호 정리
- 문장 단위 가독성 개선

### 금지되는 작업

- 말하지 않은 개념 추가
- 잘못 말한 내용을 임의로 정정
- 새로운 예시 추가
- 답변 임의 요약
- 중복을 제외한 내용 임의 삭제
- 문맥상 자연스럽게 보이도록 내용 재작성
- 확실하지 않은 단어 임의 수정
- 문장 순서와 논리 구조 임의 변경
- 원문에 존재하지 않는 정보 포함


확실하지 않은 단어는 임의로 수정하지 말고 원문 표현을 유지한다.
전문 용어로 보정할 수 있더라도, 원문 발화와의 대응이 불분명하면 보정하지 않는다.

## 3. 평가 점수 체계

최종 점수는 100점 만점으로 산정.

| 평가 영역 | 배점 |
| --- | ---: |
| 정확도 | 40 |
| 구조적 이해도 | 20 |
| 충족도 | 40 |
| 총점 | 100 |

평가 기준은 모델 변경 후에도 비교할 수 있도록 항상 동일하게 유지한다.

## 4. 평가 영역별 루브릭

### 4.1 정확도

정확도는 사용자가 핵심 단어를 잘 파악하고 설명했는지 평가한다.

핵심 단어 판단은 `term_db`를 기준으로 한다.

| 단계 | 기준 |
| ---: | --- |
| 0단계 | 핵심 단어를 전혀 언급하지 않음 |
| 1단계 | 핵심 단어를 언급함 |
| 2단계 | 핵심 단어의 기본 개념을 설명함 |
| 3단계 | 핵심 단어와 관련된 개념도 설명함 |
| 4단계 | 핵심 단어를 적절한 맥락과 관계 속에서 정확하게 설명함 |

정확도 점수는 핵심 단어별 단계 평가를 바탕으로 40점 만점으로 환산한다.

### 4.2 구조적 이해도

구조적 이해도는 사용자가 개념 간 관계를 올바르게 이해하고 설명했는지 평가한다.
관계에는 인과 관계, 목적 관계, 차이, 포함 관계, 순서, 구성 요소 간 관계 등이 포함된다.

| 단계 | 기준 |
| ---: | --- |
| 0단계 | 단어를 언급하지만 관계를 설명하지 못함 |
| 1단계 | 개념 사이의 관계를 설명하지만 불완전하거나 모호함 |
| 2단계 | 인과, 목적, 차이, 포함 관계 등을 정확하게 설명함 |
| 3단계 | 여러 개념의 관계를 일관된 논리 구조로 정확하게 설명함 |

구조적 이해도 점수는 개념 관계 설명의 정확성과 논리적 일관성을 바탕으로 20점 만점으로 환산한다.

### 4.3 충족도

충족도는 사용자가 전체 학습 범위 중 어느 정도를 다루었는지 평가한다.

RAG가 제공한 `reference_context`와 `term_db`를 기준으로 주요 주제의 누락 여부를 판단한다.

| 단계 | 기준 |
| ---: | --- |
| 0단계 | 전체 주제 중 극히 일부만 언급 |
| 1단계 | 주요 주제 중 일부만 제한적으로 다룸 |
| 2단계 | 전반적인 주제를 잘 설명함 |
| 3단계 | 모든 주요 주제를 빠짐없이 다루고, 핵심 영역과 보조 내용을 잘 구분하여 설명함 |

충족도 점수는 주요 주제 커버리지와 설명 균형을 바탕으로 40점 만점으로 환산한다.

## 5. 정량적 평가 항목

정량 지표는 전사 품질과 강의자료 기준의 내용 평가를 구분하여 사용한다.


### 5.1 Concept Recall

강의자료의 평가 대상 핵심 개념 중 사용자가 언급하고 설명한 개념의 비율이다.

Concept Recall = 설명한 핵심 개념 수 / 전체 평가 대상 핵심 개념 수

값이 높을수록 사용자가 강의자료의 핵심 내용을 많이 다룬 것으로 본다. 다만 단순 언급만으로는 충분한 설명으로 인정하지 않는다.

### 5.2 Concept Precision

사용자가 개념으로 언급한 항목 중 강의자료의 평가 대상 개념과 일치하고, 내용상 타당한 항목의 비율이다.

Concept Precision = 올바르게 언급·설명한 개념 수 / 사용자가 개념으로 언급한 항목 수

동의어, 약어, 한국어 발음, 표현 변형은 `term_db`와 RAG의 개념 정보를 활용해 판단한다. 강의자료에 없다는 이유만으로 외부 개념을 자동 오답 처리하지 않고, 주제와 무관하거나 강의 내용과 충돌하는 경우에만 precision을 낮춘다.

### 5.4 Concept F1 Score

`Concept Recall`과 `Concept Precision`의 조화 평균이다.

Concept F1 = 2 x Precision x Recall / (Precision + Recall)

핵심 개념을 많이 언급하기만 하거나, 일부 개념만 정확하게 말하는 편향을 줄이기 위해 사용한다.

### 5.5 보조 지표

필요한 경우 다음 지표를 함께 제공할 수 있다.

- `relation_recall`: 평가 대상 개념 관계 중 정확하게 설명한 관계의 비율
- `topic_coverage`: 평가 대상 주요 주제 중 다룬 주제의 비율
- `supported_claim_ratio`: 사용자의 주장 중 RAG reference context로 근거를 확인할 수 있는 주장 비율

이 지표들은 정확도, 충족도, 구조적 이해도의 루브릭 점수를 보조하며 최종 점수를 자동으로 대체하지 않는다.

### 5.6 루브릭 점수

다음 점수를 함께 제공한다.

- 정확도 점수
- 충족도 점수
- 구조적 이해도 점수
- 최종 점수

각 영역은 0~4단계로 평가하고 다음 방식으로 배점을 환산한다.

```text
정확도 점수 = 정확도 단계 / 4 x 40
충족도 점수 = 충족도 단계 / 4 x 40
구조적 이해도 점수 = 구조적 이해도 단계 / 4 x 20
최종 점수 = 세 영역 점수의 합
```

각 점수는 반드시 해당 루브릭 단계와 판단 근거를 함께 제시한다.

## 6. 정성적 평가 항목

정성 피드백은 정량 점수의 근거를 설명하고, 사용자가 다음 복습에서 개선할 방향을 제시하는 데 사용한다.

### 6.1 누락된 개념

`core_concepts`와 `reference_context`를 기준으로 사용자가 언급하지 않았거나 충분히 설명하지 않은 핵심 개념을 제시한다. 단순히 RAG 검색 결과에 존재한다는 이유만으로 누락으로 판단하지 않고, 실제 평가 대상 범위에 포함되는지 확인한다.

### 6.2 잘못 설명한 개념

사용자가 개념의 정의, 조건, 특징 또는 기능을 강의자료와 다르게 설명한 부분을 제시한다.

### 6.3 잘못 연결된 개념

개념 간 관계를 잘못 설명했거나, 인과 관계·포함 관계·절차·차이 등을 혼동한 부분을 지적한다.

### 6.4 복습 방향

누락되거나 잘못 설명한 개념과 관계를 어떤 순서로 다시 학습하면 좋을지 제시한다.

복습 방향은 강의자료와 사용자의 실제 발화에 근거해야 한다. 피드백을 위해 새로운 지식, 예시, 설명을 임의로 추가하거나 점수를 사후 보정해서는 안 된다.

## 7. 평가 출력 형식

평가 모델은 다음 JSON 형식으로 결과를 반환한다.

```json
{
  "quantitative": {
    "corrected_cer": null,
    "concept_recall": 0.0,
    "concept_precision": 0.0,
    "concept_f1": 0.0,
    "relation_recall": 0.0,
    "topic_coverage": 0.0,
    "scores": {
      "accuracy": {
        "score": 0,
        "max_score": 40,
        "rubric_level": 0,
        "reason": ""
      },
      "coverage": {
        "score": 0,
        "max_score": 40,
        "rubric_level": 0,
        "reason": ""
      },
      "structural_understanding": {
        "score": 0,
        "max_score": 20,
        "rubric_level": 0,
        "reason": ""
      },
      "total": {
        "score": 0,
        "max_score": 100
      }
    }
  },
  "qualitative": {
    "missing_concepts": [],
    "incorrect_concepts": [],
    "misconnected_concepts": [],
    "review_suggestions": []
  }
}
```

## 8. 평가 시 주의사항

- 후처리된 텍스트에 없는 정보를 근거로 점수를 부여하지 않는다.
- RAG 검색 결과에 있는 내용이더라도 사용자가 말하지 않았다면 사용자의 답변으로 간주하지 않는다.
- LLM 보정이 추가한 것으로 의심되는 개념은 평가 근거로 사용하지 않는다.
- 핵심 단어를 단순 나열한 경우에는 정확도 1단계 이상으로 과대평가하지 않는다.
- 개념 설명이 맞더라도 관계 설명이 부족하면 구조적 이해도 점수를 제한한다.
- 일부 핵심 개념을 깊게 설명했더라도 전체 범위를 다루지 못했다면 충족도 점수를 제한한다.
- 정답 전사문이 없으면 `corrected_cer`를 임의로 계산하거나 0으로 채우지 않고 `null`로 반환한다.
- RAG의 PDF JSON은 Concept Recall/Precision/F1과 루브릭 판단의 기준으로 사용하되 CER의 정답 전사문으로 사용하지 않는다.
- 피드백은 사용자의 발화 내용에 근거하여 작성한다.
