from __future__ import annotations

import json
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
PROCESSED_PATH = ROOT / "data/processed/docker.json"
RUBRIC_PATH = ROOT / "data/evaluation/rubrics/docker.json"


def T(i, ko, en="", abbr=None, aliases=None):
    return {"term_id": i, "canonical_ko": ko, "canonical_en": en, "abbreviations": abbr or [], "accepted_aliases": aliases or [], "symbols": [], "not_equivalent_to": []}


TERMINOLOGY = [
    T("docker", "Docker", "Docker"), T("containerization", "컨테이너화", "containerization"), T("container", "컨테이너", "container"), T("image", "이미지", "image"), T("dockerfile", "Dockerfile", "Dockerfile"), T("virtual_machine", "가상 머신", "virtual machine", ["VM"]), T("registry", "이미지 레지스트리", "image registry"), T("docker_hub", "Docker Hub", "Docker Hub"), T("client", "Docker 클라이언트", "Docker client", ["CLI"]), T("daemon", "Docker 데몬", "Docker daemon", ["dockerd"]), T("docker_engine", "Docker Engine", "Docker Engine"), T("docker_desktop", "Docker Desktop", "Docker Desktop"), T("immutable", "불변", "immutable"), T("build", "빌드", "build"), T("tag", "태그", "tag"), T("push", "푸시", "push"), T("pull", "풀", "pull"), T("container_run", "컨테이너 실행", "docker run"), T("from", "FROM 명령", "FROM"), T("workdir", "WORKDIR 명령", "WORKDIR"), T("copy", "COPY 명령", "COPY"), T("dockerfile_run", "RUN 명령", "RUN"), T("cmd", "CMD 명령", "CMD"), T("entrypoint", "ENTRYPOINT 명령", "ENTRYPOINT"), T("layer", "이미지 레이어", "image layer"), T("cache", "빌드 캐시", "build cache"), T("port_mapping", "포트 매핑", "port mapping", aliases=["-p"]), T("environment_variable", "환경 변수", "environment variable", aliases=["env"]), T("bind_mount", "바인드 마운트", "bind mount", aliases=["호스트 디렉터리 마운트"]), T("volume", "볼륨", "volume"), T("ephemeral", "임시적 상태", "ephemeral"), T("writable_layer", "컨테이너 쓰기 가능 레이어", "container writable layer"), T("docker_ps", "docker ps", "docker ps"), T("logs", "docker logs", "docker logs"), T("inspect", "docker inspect", "docker inspect"), T("stop", "docker stop", "docker stop"), T("remove", "docker rm", "docker rm"), T("bridge_network", "브리지 네트워크", "bridge network"), T("dns", "내장 DNS", "built-in DNS"), T("service_name", "서비스 이름", "service name"), T("compose", "Docker Compose", "Docker Compose"), T("compose_yaml", "compose.yaml", "compose.yaml"), T("service", "서비스", "service"), T("depends_on", "depends_on", "depends_on"), T("healthcheck", "healthcheck", "healthcheck"), T("restart_policy", "재시작 정책", "restart policy"), T("compose_down_v", "docker compose down -v", "docker compose down -v")
]

TITLES = ["Docker 표지", "강의 목차", "Introduction", "컨테이너화", "Docker 객체", "Docker 객체 비유", "Docker Hub", "Docker Architecture", "환경 공유 방식", "Docker 구성요소", "Client-Server Architecture", "Docker Desktop", "Docker Engine", "Dockerfile", "Dockerfile 주요 명령", "이미지 빌드와 레이어", "컨테이너 실행 옵션", "컨테이너와 이미지 관리", "Docker Volume", "실습 개요", "실습 저장소", "이미지 빌드 실습", "태그와 Push 실습", "Docker Hub 확인", "Pull과 Run 실습", "컨테이너 점검 명령", "Frontend 실습", "Frontend 실행 확인", "Docker Compose", "Docker Compose 도입", "Docker Bridge Network", "Docker Compose 목적과 명령", "멀티 컨테이너 구성", "compose.yaml 구성", "Compose 실행과 종료", "강의 마무리"]
CONTENT = {
1:"Docker 강의 표지이다.",2:"강의는 Docker 소개, 구조와 객체, Docker Compose 순으로 구성된다.",3:"Docker 소개 섹션의 구분 페이지이다.",4:"컨테이너화는 애플리케이션과 실행 환경을 함께 묶어 환경 차이 문제를 줄인다. 컨테이너는 일반적으로 VM보다 가볍고 빠르게 시작한다.",5:"Dockerfile은 이미지 생성 지시사항을 기록하고, image는 애플리케이션과 환경을 담은 불변 패키지이며, container는 image의 실행 인스턴스다. build·push·run으로 이어진다.",6:"Dockerfile·image·container의 관계를 조리법·완성 음식·제공된 음식에 빗대어 보여준다.",7:"Docker Hub는 image를 push·pull하여 공개 또는 비공개로 저장·공유하는 registry다.",8:"Docker architecture 섹션의 구분 페이지이다.",9:"Docker는 전체 개발 환경 대신 애플리케이션 실행에 필요한 환경을 container 단위로 공유한다.",10:"Docker client, host의 daemon·images·containers, registry 사이의 관계를 보여준다.",11:"사용자는 Docker client로 명령하고 dockerd가 build·run·pull 등을 처리한다. pull은 registry의 image를 daemon이 받아오며 daemon은 중앙 제어 백그라운드 프로세스다.",12:"Docker Desktop은 Docker를 편리하게 관리하는 선택적 GUI다.",13:"Docker Engine 설치 후 hello-world container를 실행해 동작을 확인한다.",14:"Dockerfile 설명의 구분 페이지이다.",15:"FROM은 base image, WORKDIR은 기본 작업 디렉터리, COPY는 파일 포함, RUN은 build 중 shell 명령, CMD·ENTRYPOINT는 container 시작 시 기본 명령을 설정한다.",16:"Image는 Dockerfile 명령에 따른 layer로 구성되고 cache를 이용해 변경된 layer 중심으로 재빌드한다. build 시 이름과 tag를 붙이고 registry에 push할 수 있다.",17:"docker run에서 -p는 host와 container port를 연결하고, --env-file·-e는 환경 변수를 전달하며, -v host:container는 host 경로를 mount한다.",18:"docker ps와 ps -a로 container를 확인하고 stop·rm으로 정지·삭제한다. Dockerfile 변경 후 image를 rebuild하며 images·rmi로 image를 확인·삭제한다.",19:"Container의 writable layer는 영속 저장소로 간주하면 안 된다. Container 제거·재생성 뒤에도 보존할 데이터는 host의 volume이나 bind mount에 저장한다. 단순 restart만으로 writable layer가 초기화되지는 않는다.",20:"Dockerfile build, Docker Hub push, image pull과 container run, 실행 container 점검으로 이어지는 실습 순서를 제시한다.",21:"Docker Compose 실습 저장소 링크를 제공한다.",22:"Dockerfile 디렉터리에서 docker build -t image_name . 으로 현재 directory를 build context로 사용한다.",23:"docker tag로 repository·image·tag 이름을 붙이고 docker push로 Docker Hub에 업로드한다.",24:"Push한 image가 Docker Hub repository에 등록되었는지 확인한다.",25:"docker run의 --rm은 종료 후 자동 삭제, -d는 백그라운드 실행, -p는 host:container port mapping이다.",26:"docker ps·logs·inspect·stop·rm으로 실행 상태, log, 상세 정보, 정지와 삭제를 관리한다.",27:"Frontend Dockerfile에도 동일한 build·tag·push·run 절차를 적용하는 실습이다.",28:"localhost:3000에서 frontend container의 실행 결과를 확인한다.",29:"Docker Compose 섹션 구분 페이지이다.",30:"Docker Compose 도입 페이지이다.",31:"Bridge network는 격리된 container들이 통신하는 가상 내부망이며 built-in DNS를 통해 바뀔 수 있는 IP 대신 service name으로 찾게 한다.",32:"Docker Compose는 compose.yaml에 여러 container service와 관계를 선언하고 multi-container app을 함께 build·up·down하는 도구다. Dockerfile의 변형이 아니라 여러 service를 구성·실행하는 별도 계층이다.",33:"Frontend·backend·database 등 여러 container가 network와 volume으로 연결되는 구성을 보여준다.",34:"compose.yaml의 services는 container를, volumes는 persistent storage를, networks는 통신망을 정의한다. depends_on은 의존·시작 순서를 표현하지만 readiness 자체를 보장하지 않아 healthcheck 조건을 함께 고려한다. restart는 재시작 정책이다.",35:"docker compose up --build로 build와 실행을 하고 down으로 구성 자원을 내린다. down -v는 연결된 volume까지 삭제하므로 데이터 삭제 의도를 확인해야 한다.",36:"Docker 강의 종료 페이지이다."}
DIV={3,8,14,29,30}; ROLES={1:"cover",2:"table_of_contents",36:"closing",**{p:"section_divider" for p in DIV}}
def cur(p):
    role=ROLES.get(p,"core_content"); title=TITLES[p-1]
    return [{"topic":title,"concepts":[title],"visual_description":f"{title}의 명령·구조·예시를 제시한다." if role=="core_content" else f"{title} 페이지이다.","content":CONTENT[p]}]
CURATION={p:cur(p) for p in range(1,37)}

def U(i,p,k,q,x,ts,st="text"):
    return {"unit_id":i,"page":p,"type":k,"source_type":st,"source_excerpt":q,"normalized_explanation":x,"source_status":"verified","term_ids":ts}
UNITS=[
U("docker_p4_containerization",4,"comparison","소프트웨어와 실행에 필요한 모든 환경을 상자처럼 하나로 묶어 어디서든 동일하게 실행","컨테이너화는 앱과 실행 환경을 묶어 환경 일관성을 높이며 VM보다 가벼운 격리 방식을 제공한다.",["containerization","container","virtual_machine"]),
U("docker_p5_objects",5,"relation","Dockerfile로부터 Docker Image를 build하여 registry에 push하고 Image로부터 Docker Container를 run","Dockerfile→image→container와 build·push·run 관계다.",["dockerfile","image","container","build","push","container_run","registry","immutable"]),
U("docker_p7_registry",7,"definition","Docker Image를 저장·공유하는 공식 Image Registry / push / pull","Docker Hub는 image registry다.",["docker_hub","registry","image","push","pull"]),
U("docker_p10_components",10,"relation","Client / Docker Host / Docker daemon / Containers / Images / Registry","Docker 주요 component와 통신 관계다.",["client","daemon","container","image","registry"],"visual"),
U("docker_p11_daemon",11,"procedure","클라이언트 docker를 통한 명령을 docker daemon(dockerd)이 실행","Client 명령을 daemon이 처리하고 registry와 image를 교환한다.",["client","daemon","pull","build","container_run","registry"]),
U("docker_p15_instructions",15,"procedure","FROM / WORKDIR / COPY / RUN / CMD / ENTRYPOINT","Dockerfile build 명령과 실행 기본 명령의 역할을 구분한다.",["dockerfile","from","workdir","copy","dockerfile_run","cmd","entrypoint"]),
U("docker_p16_layers",16,"procedure","각 명령어가 새로운 레이어 생성 / 캐시를 활용해 변경된 레이어만 수정","Image layer와 cache 기반 rebuild를 설명한다.",["image","layer","cache","build","tag","push"]),
U("docker_p17_run",17,"procedure","docker run -p / --env-file / -e / -v 호스트경로:컨테이너경로","Container 실행 시 port·environment·mount를 전달한다.",["container_run","port_mapping","environment_variable","bind_mount"]),
U("docker_p18_manage",18,"procedure","docker ps / stop / rm / images / rmi / build","Container와 image lifecycle 관리 명령이다.",["docker_ps","stop","remove","image","build"]),
U("docker_p19_persistence",19,"warning","컨테이너는 기본적으로 Ephemeral / Volume에 연결된 데이터는 안전하게 보존","Container writable layer를 영속 저장소로 보지 않고 volume을 사용한다. 단순 restart는 layer를 지우지 않는다.",["ephemeral","writable_layer","volume","container"]),
U("docker_p25_flags",25,"procedure","--rm: 종료되면 자동 삭제 / -d: 백그라운드 / -p: 포트 매핑","docker run 주요 flag를 구분한다.",["container_run","remove","port_mapping"]),
U("docker_p26_debug",26,"procedure","docker logs / docker inspect / docker stop / docker rm","Log·상세 정보·정지·삭제 명령으로 container를 점검한다.",["logs","inspect","stop","remove","docker_ps"]),
U("docker_p31_network",31,"definition","Network: 컨테이너끼리 데이터를 주고받는 가상 내부망 / Built-in DNS / 서비스 이름","Bridge network와 DNS service discovery를 설명한다.",["bridge_network","dns","service_name","container"]),
U("docker_p32_compose",32,"definition","멀티 컨테이너로 구성된 앱을 구동 / compose.yaml / build up down","Compose는 여러 service를 선언·실행하는 도구다.",["compose","compose_yaml","service","build"]),
U("docker_p34_schema",34,"procedure","services / volumes / networks / depends_on / healthcheck / restart","Compose 구성 필드의 역할을 구분한다.",["compose_yaml","service","volume","bridge_network","depends_on","healthcheck","restart_policy"]),
U("docker_p35_down",35,"warning","docker compose down -v (volume까지 삭제)","down -v는 volume과 그 데이터를 삭제할 수 있다.",["compose","compose_down_v","volume"])]
MAP={"docker.containerization":["docker_p4_containerization"],"docker.registry":["docker_p7_registry"],"docker.objects":["docker_p5_objects"],"docker.components":["docker_p10_components"],"docker.daemon":["docker_p11_daemon"],"docker.daemon_registry":["docker_p11_daemon"],"docker.client_server":["docker_p10_components","docker_p11_daemon"],"docker.instructions":["docker_p15_instructions"],"docker.start_command":["docker_p15_instructions"],"docker.layers":["docker_p16_layers"],"docker.distribution":["docker_p16_layers"],"docker.cache_rebuild":["docker_p16_layers"],"docker.run_flags":["docker_p25_flags"],"docker.workflow":["docker_p5_objects"],"docker.run_options":["docker_p17_run"],"docker.bind_mount":["docker_p17_run"],"docker.lifecycle":["docker_p18_manage"],"docker.debug":["docker_p26_debug"],"docker.persistence":["docker_p19_persistence"],"docker.volume_delete":["docker_p35_down"],"docker.restart_persistence":["docker_p19_persistence"],"docker.network":["docker_p31_network"],"docker.service_discovery":["docker_p31_network"],"docker.compose":["docker_p32_compose"],"docker.compose_commands":["docker_p32_compose"],"docker.compose_schema":["docker_p34_schema"],"docker.compose_cleanup":["docker_p35_down"],"docker.health_restart":["docker_p34_schema"]}
ERR={"docker.objects":["Image와 container를 같은 실행 중 객체라고 설명"],"docker.instructions":["RUN이 container 시작 시마다 실행되는 기본 명령이라고 설명"],"docker.persistence":["단순 restart만 해도 container writable layer가 반드시 초기화된다고 설명"],"docker.network":["Container가 service name 대신 변동 IP로만 통신할 수 있다고 설명"],"docker.compose":["Docker Compose가 Dockerfile의 문법 변형이라고 설명"],"docker.compose_schema":["depends_on만으로 dependency service의 application readiness까지 보장된다고 설명"],"docker.volume_delete":["down -v가 volume 데이터를 보존한다고 설명"]}
def C(i,role,text,cat="explanation_application"):
    return {"claim_id":i,"role":role,"category":cat,"text":text,"weight":1.0,"evidence":[],"term_ids":[],"evaluation_criteria":{"required_elements":[text],"critical_errors":ERR.get(i,[])}}
def S(i,t,x,c): return {"sub_objective_id":i,"title":t,"summary":x,"claims":c}
def O(i,t,x,s):
    n=sum(len(v["claims"]) for v in s); return {"objective_id":i,"title":t,"selection_description":x,"supporting_claim_slots":2 if n<=8 else 3,"sub_objectives":s}
def build_rubric():
    objectives=[
    O("docker.foundations","컨테이너화와 Docker 객체","Docker가 해결하는 문제와 핵심 객체·구조를 설명한다.",[
      S("docker.foundation.containerization","컨테이너화","VM과 비교해 목적을 설명한다.",[C("docker.containerization","essential","Containerization은 application과 실행 environment를 함께 묶어 환경 차이를 줄이며 일반적으로 VM보다 가볍게 격리한다.","core_understanding"),C("docker.registry","supporting","Docker Hub 같은 registry는 image를 push·pull하여 저장·배포·재사용하게 한다.")]),
      S("docker.foundation.objects","Docker 객체와 생명주기","Dockerfile·image·container를 연결한다.",[C("docker.objects","essential","Dockerfile은 image build 지시사항이고 image는 immutable package이며 container는 image의 실행 instance다."),C("docker.components","supporting","Docker client·daemon·images·containers·registry가 명령과 image 전달로 연결된다.")]),
      S("docker.foundation.architecture","Client-Server 구조","명령 처리 주체를 설명한다.",[C("docker.daemon","essential","사용자는 Docker client로 명령하고 dockerd가 build·run·pull을 처리하며 registry와 통신한다."),C("docker.daemon_registry","supporting","docker pull 요청 시 daemon은 registry에서 image를 받아 host에 저장한다."),C("docker.client_server","supporting","Docker client는 요청 인터페이스이고 daemon은 images와 containers를 실제로 관리하는 server 역할을 한다.")])]),
    O("docker.image_build","Dockerfile과 Image Build","Dockerfile instruction과 layer·cache를 설명한다.",[
      S("docker.build.instructions","Dockerfile 명령","Build-time과 start-time 역할을 구분한다.",[C("docker.instructions","essential","FROM·WORKDIR·COPY·RUN은 image build 내용을 정의하고 CMD·ENTRYPOINT는 container 시작 시 기본 실행 명령을 설정한다.","core_understanding"),C("docker.start_command","supporting","RUN은 image build 중 실행되고 CMD·ENTRYPOINT는 container가 시작될 때의 기본 process를 정한다.")]),
      S("docker.build.layers","Layer와 Cache","재빌드 원리를 설명한다.",[C("docker.layers","essential","Docker image는 instruction별 layer로 구성되며 cache를 이용해 변경된 layer 이후를 중심으로 rebuild한다."),C("docker.distribution","supporting","Build한 image에 repository와 tag를 붙여 registry에 push할 수 있다."),C("docker.cache_rebuild","supporting","Dockerfile 앞쪽 layer가 바뀌면 그 뒤 layer의 cache도 다시 계산될 수 있어 instruction 순서가 rebuild 효율에 영향을 준다.")]),
      S("docker.build.workflow","Build·Tag·Push","배포 흐름을 설명한다.",[C("docker.run_flags","essential","Image를 pull한 뒤 docker run의 -d·--rm·-p 등으로 실행 방식을 지정할 수 있다."),C("docker.workflow","supporting","Build·tag·push·pull·run은 Dockerfile에서 배포된 container까지의 흐름을 이룬다.")])]),
    O("docker.container_operations","Container 운영과 데이터 영속성","실행 옵션·진단·persistent data를 설명한다.",[
      S("docker.ops.run","실행 옵션","Port·environment·mount를 구분한다.",[C("docker.run_options","essential","docker run의 -p는 port mapping, -e·--env-file은 environment 전달, -v host:container는 bind mount를 설정한다.","core_understanding"),C("docker.bind_mount","supporting","Bind mount는 host path를 container path에 연결해 host와 파일을 공유한다.")]),
      S("docker.ops.lifecycle","상태와 진단","명령별 역할을 설명한다.",[C("docker.lifecycle","essential","docker ps·ps -a로 container 상태를 보고 stop·rm으로 정지·삭제하며 images·rmi로 image를 관리한다."),C("docker.debug","supporting","docker logs는 log를, inspect는 container나 image의 상세 정보를 보여준다.")]),
      S("docker.ops.persistence","영속성","Ephemeral layer와 volume을 구분한다.",[C("docker.persistence","essential","Container writable layer는 영속 저장소로 보장되지 않으므로 제거·재생성 뒤에도 필요한 DB·log·upload data는 volume이나 bind mount에 둔다."),C("docker.volume_delete","supporting","docker compose down -v는 volume까지 삭제할 수 있으므로 persistent data 삭제 의도를 확인해야 한다."),C("docker.restart_persistence","supporting","단순 container restart는 writable layer를 지우지 않지만 제거·재생성에 대비한 영속 data는 volume에 저장해야 한다.")])]),
    O("docker.compose_networking","Network와 Docker Compose","Multi-container app의 연결과 선언을 설명한다.",[
      S("docker.compose.network","Bridge와 DNS","Service discovery를 설명한다.",[C("docker.network","essential","Bridge network는 container 간 가상 내부망이며 built-in DNS가 변동 IP 대신 service name으로 서로 찾게 한다.","core_understanding"),C("docker.service_discovery","supporting","같은 Docker network의 service는 db·redis 같은 service name으로 상대를 찾을 수 있다.")]),
      S("docker.compose.purpose","Compose 목적","Dockerfile과 Compose를 구분한다.",[C("docker.compose","essential","Docker Compose는 Dockerfile의 변형이 아니라 compose.yaml로 여러 service·network·volume을 선언하고 함께 build·up·down하는 도구다."),C("docker.compose_commands","supporting","docker compose build·up·down은 multi-container 구성을 build·시작·종료한다.")]),
      S("docker.compose.schema","Compose 구성과 readiness","주요 field와 실행 주의를 설명한다.",[C("docker.compose_schema","essential","compose.yaml의 services·volumes·networks가 실행 객체와 연결을 정의하고 depends_on은 순서를 표현하지만 readiness에는 healthcheck 조건이 필요하다."),C("docker.compose_cleanup","supporting","docker compose down은 구성을 내리고 down -v는 volume까지 삭제한다."),C("docker.health_restart","supporting","healthcheck는 service health를 판정하고 restart field는 container failure 뒤 재시작 정책을 설정한다.")])])]
    return {"schema_version":"2.2.0","lecture_id":"docker","lecture_name":"Docker","assessment":{"mode":"selected_topic_recall","target_seconds":120,"max_seconds":120,"score_policy":{"essential_points":60,"supporting_points":20,"coverage_points":20}},"top_level_objectives":objectives,"excluded_source_claims":[{"page":19,"chunk_id":"docker_p19_01","source_text":"컨테이너를 삭제하거나 재시작하면, 내부 파일도 함께 초기화","reason":"단순 restart는 writable layer를 지우지 않으므로 제거·재생성과 구분"},{"page":32,"chunk_id":"docker_p32_01","source_text":"여러 docker file 관리하기 위한 docker file 의 변주","reason":"Compose는 Dockerfile 변형이 아니라 multi-container service를 구성·실행하는 별도 도구"}]}

def apply_evaluation_data(processed_path=PROCESSED_PATH,rubric_path=RUBRIC_PATH):
    d=json.loads(Path(processed_path).read_text()); pages={x["page"]:x for x in d["chunks"]}
    if set(pages)!=set(range(1,37)): raise ValueError("Docker PDF 1~36쪽이 필요합니다.")
    d["schema_version"]="2.1.0"; d["terminology"]=TERMINOLOGY
    for p in pages: pages[p].update(page_role=ROLES.get(p,"core_content"),term_ids=[],evidence_units=[],source_issues=[])
    pages[19]["source_issues"]=[{"issue_id":"docker_p19_restart","source_text":"컨테이너를 삭제하거나 재시작하면, 내부 파일도 함께 초기화","issue_type":"incorrect","correction":"단순 restart는 writable layer를 보존한다. 제거·재생성 시 내부 writable layer가 사라질 수 있으므로 volume을 사용한다.","evaluation_policy":"exclude"}]
    pages[32]["source_issues"]=[{"issue_id":"docker_p32_compose_variant","source_text":"여러 docker file 관리하기 위한 docker file 의 변주","issue_type":"incorrect","correction":"Compose는 compose.yaml로 multi-container service를 구성·실행하며 Dockerfile의 변형이 아니다.","evaluation_policy":"exclude"}]
    pages[34]["source_issues"]=[{"issue_id":"docker_p34_depends_readiness","source_text":"depends_on : 서비스 간 실행 순서 정의","issue_type":"ambiguous","correction":"depends_on은 dependency order를 표현하지만 readiness 보장은 healthcheck 조건을 함께 사용해야 한다.","evaluation_policy":"warn"}]
    lookup={}
    for raw in UNITS:
        z=dict(raw); p=z.pop("page"); pages[p]["term_ids"]=list(dict.fromkeys(pages[p]["term_ids"]+z["term_ids"])); pages[p]["evidence_units"].append(z); lookup[z["unit_id"]]=(pages[p],z)
    Path(processed_path).write_text(json.dumps(d,ensure_ascii=False,indent=2)+"\n")
    r=build_rubric(); claims={c["claim_id"]:c for o in r["top_level_objectives"] for s in o["sub_objectives"] for c in s["claims"]}
    if set(claims)!=set(MAP): raise ValueError(f"Claim mismatch: {set(claims)^set(MAP)}")
    for cid,uids in MAP.items():
        terms=[]
        for uid in uids:
            ch,u=lookup[uid]; terms+=u["term_ids"]; claims[cid]["evidence"].append({"page":ch["page"],"chunk_id":ch["chunk_id"],"unit_id":uid,"source_excerpt":u["source_excerpt"],"source_status":"verified","review_note":""})
        claims[cid]["term_ids"]=list(dict.fromkeys(terms))
    Path(rubric_path).parent.mkdir(parents=True,exist_ok=True); Path(rubric_path).write_text(json.dumps(r,ensure_ascii=False,indent=2)+"\n")

if __name__=="__main__": apply_evaluation_data()
